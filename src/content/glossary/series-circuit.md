---
term: "Series Circuit"
category: "circuits"
relatedTerms: ["parallel-circuit", "circuit"]
relatedLessons: ["circuits/series-circuits", "circuits/series-vs-parallel"]
---

A series circuit connects components end-to-end so that current follows a single path through every component. The same current flows through each one, but the voltage divides among them. If any component fails or is removed, the entire circuit stops working because the single path is broken.
